<div class="w-full">
  <%= turbo_frame_tag 'game_main' do %>
    <div class="flex flex-col justify-between items-center">
    <%= render "form", game: @game %>
    <%= link_to 'スタート画面にもどる', games_path, class: "btn btn-secondary btn-wide" %>
    </div>
  <% end %>
</div>
