<div id="<%= dom_id owner %>">
  <% if action_name != "show" %>
    <%= link_to "Show this owner", game_owner_path(game, owner), class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to 'Edit this owner', edit_game_owner_path(game, owner), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
