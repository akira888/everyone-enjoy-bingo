<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex flex-col justify-between items-center">
    <h1 class="font-bold text-4xl">主催者情報</h1>
    <p>登録が完了していないようです。以下のボタンから、主催者情報を登録して、ゲームを開始しましょう！</p>
    <%= link_to 'New owner', new_game_owner_path(@game), class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>
</div>
